// var, let, const

let myName = Sophia;

function sayHello() {
  console.log("Mein Name ist " + myName);
}

sayHello();

// const - kann nicht mehr geaendert werden
// var - alt (soll fast nie verwendet werden). Eine mit "var" deklarierte Variable, die in einer Funktion steht, ist, auch, wenn sie in einer Funktion steht, ueberall gueltig
